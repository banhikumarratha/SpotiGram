[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "streamlit-template"
version = "0.1.0"
description = "Template UI service for SpotiGram"
dependencies = [
    "streamlit>=1.36.0",
    "requests>=2.32.0",
    "spotigram-shared @ file://../../packages/shared"
]
